import type { Options } from './rollGameTypes';

export const DEFAULT_SELECTED_PLATFORMS = ['playstation', 'playstation-2'];

export const DEFAULT_OPTIONS: Options = {
  secondsToSpin: 30,
  speed: 3,
  numberOfGames: 16,
  theme: 'dark',
};
